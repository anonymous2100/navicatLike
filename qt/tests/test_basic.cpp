#include <gtest/gtest.h>
#include <QtTest>

TEST(BasicTest, AppName)
{
    EXPECT_TRUE(true);
}

TEST(BasicTest, DbTypeConversion)
{
    // Placeholder: verify DbType from/to string conversion
    // These tests require linking against the main library
    EXPECT_TRUE(true);
}
